'use client';

/**
 * GestureBar Component
 * 
 * Mobile-style gesture/swipe indicator at bottom.
 * Demo mode - just for UI presentation.
 */
export default function GestureBar() {
  return (
    <div className="gesture-bar">
      <div className="gesture-handle" />
    </div>
  );
}
